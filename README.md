# CarPricePrediction
using Machine learning and deepLearning

first ly open the commad promt in ur pc.
open the folder of this project
the type code . in commad prompt.
then it will open the folder in ur vs code.
In vs code open the terminal.
> install the necessary packages using pip install..
> type " pip install numpy pandas " 
> next type " pip install scikit-learn "
> next type " pip install scikit-learn "
> once all the necessary packages are installed run the "app.py" file. if their is no error.
> run streamlit run app.py in ur terminal

 thank you 
 love coding with Pluggy....
